<article class='bg-slate-50 border border-slate-300 rounded-md p-4 lg:w-1/3 md:w-2/3 sm:w-full'>
  <div>
  <%#= link_to insta_user_post_path(post.insta_user, post) do %>
  <%# end %>
    <% if post.image? || post.carousel_album? %>
      <%= image_tag post.media_url %>
    <% elsif post.video? %>
      <%= video_tag post.media_url, poster: post.thumbnail_url, controls: true %>
    <% end %>
  </div>
  <div>
    <p class='break-normal'>
      <%= highlight(post.caption, params[:caption]) %>
    </p>
    <p>
      🕚 <%= post.timestamp.to_fs(:long) %>
    </p>
    <p>
      🔗 <%= link_to post.remote_id, post.permalink, target: '_blank' %>
    </p>
  </div>
</article>
